function solve(n){

    for (let i = 1; i <= n; i++) {
        
        let str = "";
        for (let j = 1; j <= n; j++) {
            
            str += `${n} `;
            
        }

        console.log(str);
        
    }
}

solve(3);